/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // webpack:(config, {isServer})=>{
    //     if(!isServer){
    //         config.resolve={
    //             ...config.resolve,
    //             fallback:{
    //                 fs: false,
    //                 ChildProcess: false
    //             },
    //         };
    //     }
    //     return config;
    // }
};

export default nextConfig;
